# flashagent

Functional alias for the exact matching `platformio-mcp` release from https://github.com/jl-codes/platformio-mcp. It forwards commands, permissions, standard input/output and exit status to the canonical CLI.

Candidate only: packaging is prepared; publication and namespace control are not yet verified.
